#include <stdio.h>

int main() {
    int t,s; // TS... is not for the TypeScript... it's....
    // TranSformer.As you know. AS YOU KNOW.
    scanf("%d %d", &t, &s);

    if (t >= 12 && t <= 16 && !s) {
        puts("320");
    } else {
        puts("280");
    }

    return 0;
}